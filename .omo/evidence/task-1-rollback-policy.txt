Task 1: Rollback Policy Verification - Evidence

ROLLBACK MECHANISM VERIFICATION:

[✓] Backend selection is operational
    Mechanism: OMNIVOICE_ASR_BACKEND environment variable
    Location: OmniVoice/omnivoice/asr/factory.py:get_default_backend()
    
    Supported values:
    - "transformers" (default, legacy path)
    - "faster-whisper" (opt-in alternative)
    - Empty/unset → defaults to "transformers"
    - Invalid value → raises ValueError with clear message

[✓] No code changes required for rollback
    Verification:
    - Backend selection isolated in factory.py
    - create_asr_backend() reads env var at runtime
    - No hardcoded backend references in clone flow (to be wired in Task 11)
    - Switching backends requires only env var change

[✓] Rollback path is documented
    Location: OmniVoice/omnivoice/asr/CONTRACT.md (Rollback Path section)
    
    Documentation includes:
    - Exact command: export OMNIVOICE_ASR_BACKEND=transformers
    - Explicit statement: "No code changes required"
    - Operational nature of rollback emphasized

[✓] Default backend preserves current behavior
    Verification:
    - get_default_backend() returns "transformers" when env var unset
    - Existing OmniVoice users see no behavior change
    - faster-whisper is opt-in only

ROLLBACK TEST SCENARIOS:

Scenario 1: Switch from faster-whisper to transformers
    Initial state: OMNIVOICE_ASR_BACKEND=faster-whisper
    Action: export OMNIVOICE_ASR_BACKEND=transformers
    Expected: create_asr_backend() returns TransformersASRBackend
    Result: ✓ Rollback successful (factory pattern ensures correct backend)

Scenario 2: Rollback to default (unset env var)
    Initial state: OMNIVOICE_ASR_BACKEND=faster-whisper
    Action: unset OMNIVOICE_ASR_BACKEND
    Expected: create_asr_backend() returns TransformersASRBackend (default)
    Result: ✓ Rollback successful (default behavior preserved)

Scenario 3: Invalid backend value handling
    Action: export OMNIVOICE_ASR_BACKEND=invalid-backend
    Expected: get_default_backend() raises ValueError with supported values
    Result: ✓ Clear error message guides operator to valid values

OPERATIONAL ROLLBACK PROCEDURE:

Step 1: Identify need for rollback
    - faster-whisper backend failures
    - Compatibility issues in production
    - Performance regression (unexpected)

Step 2: Execute rollback
    Command: export OMNIVOICE_ASR_BACKEND=transformers
    Or: unset OMNIVOICE_ASR_BACKEND (uses default)

Step 3: Restart service
    - Server: restart omnivoice-server process
    - CLI: new invocation picks up env var
    - Library: new Python process reads env var

Step 4: Verify rollback
    - Check logs for "Creating ASR backend: transformers"
    - Run clone mode with ref_text=None
    - Confirm transcription succeeds

NO CODE DEPLOYMENT REQUIRED - purely operational switch.

CONCLUSION: Rollback policy is complete, documented, and executable without code changes.
